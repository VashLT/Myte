import React from 'react';
import SidePanel from '../Core/SidePanel/SidePanel';

export const Dashboard: React.FC = () => {
    return (
        <div>
            <SidePanel />
        </div>
    );
}

export default Dashboard;